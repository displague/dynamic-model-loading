# v0.29: decision-risk acquisition clears the binary-proxy screen

Fifth authorized delivery. The [frozen protocol](acquisition-screen-protocol.md)
finishes in26.592s (worker13.498s, analysis13.094s). **Hrisk, Hcommutation and
Hnumerical pass; Hcontribution fails.** Nominate risk only for a new short physical
policy screen. This is not yet an online runtime or a full-vocabulary result.

| Same17-page budget,32 diagnostic positions | Pair errors | Repairs | New errors |
|---|---:|---:|---:|
| Base, before acquisition | 7 | -- | -- |
| Fixed first17 | 2 | 7 | 2 |
| Contribution-ranked | 2 | 7 | 2 |
| Decision-risk | **0** | **7** | **0** |
| Independent-risk | 3 | 4 | 0 |

Risk has zero errors on each diagnostic document; fixed has one each. The risk
policy clears its frozen two-fewer-errors bar and beats both other adaptive
controls. Contribution merely ties fixed and fails its separate gate. Fit errors
are fixed5, contribution8, risk5, independent-risk7 out of96. The result is a
small diagnostic ranking win, not a broad claim of better learned probabilities.
Gaussian bands were undercovered in v0.28; no safety certificate is asserted.

The scored decision is the sign of an additive **base top-two margin** versus
the additive all35-page margin. It is not the complete high-draft/target argmax;
third-token changes remain unresolved until the physical/full-readout experiment.
Posterior forecasts are recorded separately and never substituted for the actual
partial-computation decisions. Only purchased scalar observations enter policy
updates. There is no recurrence or unseen-page oracle.

Projection and Gaussian conditioning commute within **1.111e-15** for means and
full page covariance across128 positions under the separable model. This qualifies
a scalar observation only for the fixed current direction, not another candidate
pair. Every available action on both diagnostic risk paths is checked against
128-point quadrature: maximum difference **1.520e-11**, below1e-4. Algebraic and
numerical passes do not certify the probabilistic model.

## Provenance and cost

Source `2eaaf9f4a342222826ce544456abd4711232ebc4`, protocol and parameters were
reviewed, validated with492 CPU tests, committed and pushed before scoring.
Nine new tests include analytic Gaussian-sign probabilities, quadrature symmetry,
last-page zero risk and purchased-only callbacks. All archived inputs, policy
paths, observations, scores and aggregates exactly replay, including a second audit.

The same96 fit positions/six documents and32 diagnostic positions/two documents
are reused development data, not independent confirmation. No new checkpoint
inference, GPU or physical H2D measurement occurs. PeakRSS220962816B; input tensors
56665088B and prediction/model tensors3113032B. Worker includes all policies,
fit, parent verification and numerical checks; it is not normal per-token latency.

Proposed17-page payload is15040512B. Scalar readback would be68B instead of104448B
of full vectors, plus6144B current-axis readback and all model/controller costs.
These are analytical costs, not observed physical savings. v0.26's original
collection remains fully paid. No accepted-token, speedup or capacity claim.

Raw ZIP41069015B,81 members, each decompressed/checked and raw source rechecked;
SHA256 `c6764010ba73f773a70b1443de909b8dfa56a9ac2fb37d8b10b14b012626b238`.
Includes source/protocol, extracted inputs, model/posterior/policy arrays, receipts
and upstream model licensing; complete original measurements remain in v0.26.

## Next

The sixth delivery must test the nominated policy on generated trajectories with
full-vocabulary draft readouts, dense target verification, explicit prefill/KV
rules, physical transfers and charged controller/memory costs. Freeze a short
protocol first. A pair-proxy win does not satisfy native admission or justify
a long matrix. Complete issue44 only; milestone10 remains open.
