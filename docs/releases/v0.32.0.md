# v0.32.0 - Physical exact-zero acquisition saves bytes, misses the latency bar

Second of six authorized deliveries. One **24.131-second** screen preserves all
tested greedy outputs while actually loading selected OPT outgoing weights.
**Htraffic passes; Hruntime fails.** The candidate saves 14.4409% weight H2D and
4.89965% wall time against streaming, just below the frozen inclusive 5% latency
bar. That narrow miss is not statistical evidence of no speedup. Preserve the
registered negative; no rerun, gate change or long matrix follows.

| Condition | Tokens | Weight H2D | Extents | Wall |
|---|---:|---:|---:|---:|
| Streaming | 16 | 25,769,803,776 B | 384 | 2.072192s |
| Exact-zero 128-row pages | 16 | 22,048,407,552 B | 2,286 | 1.970661s |

First projections and attention remain fully computed/resident. Actual nonzero
activations select outgoing-weight pages; absent weights never certify inactivity.
One shared 64 MiB GPU workspace and 64 MiB pinned staging buffer serve host-packed
weights. Dense PyTorch multiplication remains. Approximately 95.91% activation
zeros become only 14.44% physical traffic savings at this page/prefill grain.

All four hybrid episodes and two restored repeats match the original target IDs.
Maximum per-position full-logit relative L2 is 8.54746e-6 against 1e-5; restored
dense repeats are exact. Separate fresh KV is audited. This is pretrained OPT on
two short authored continuations, not instruction quality, speculation or Qwen-32B
evidence. Resident repeats take 0.177692s and remain much faster. Initial resident
1.316781s includes first-use initialization; it is not a steady-state denominator.

[Prospective protocol and inspirations](https://github.com/displague/dynamic-model-loading/blob/v0.32.0/docs/sparse-down-screen-protocol.md),
[full findings and accounting](https://github.com/displague/dynamic-model-loading/blob/v0.32.0/docs/sparse-down-screen-results.md),
[compact receipts](https://github.com/displague/dynamic-model-loading/tree/v0.32.0/results/sparse-down-screen-20260915),
[reviewed source freeze](https://github.com/displague/dynamic-model-loading/commit/12a9822a58bab6e3581c4d3c6f9f50f28cf062bf).

Worker 19.625s + independent audit 4.506s; second audit exactly reproduces the
summary without inference. Review fixes cover inclusive boundaries, serialized
workspace timestamps and allocator peaks. **566 CPU tests pass**, with final
clean-tip validation and XML attached. Peak sampled total GPU 5,919,313,920 B;
RSS 8,909,803,520 B. Hybrid allocation falls, but initial full residency and retained
allocator reservation prevent a hard-budget capacity claim. Setup copies and all
episode traffic, discovery, staging, workspace and KV are recorded separately.

Raw archive `sparse-down-v032-raw.zip`: 14,744,011 B, 124 members.
SHA256 `c59a65a806c057485fd292187a30bba20da667d18b6785fecc555e48986544ac`.
Every member decompressed/hash checked; raw sources rechecked. Pinned OPT weights
are external dependencies, not redistributed.

Complete [#47](https://github.com/displague/dynamic-model-loading/issues/47), not
[milestone 10](https://github.com/displague/dynamic-model-loading/milestone/10).
Next separately test one compact active-row packet per layer, charging gather,
indices, scatter and transfers. Four deliveries remain. No llama.cpp changes,
native admission, deployment or expanded performance suite follows this screen.
