**Findings remain that should be fixed before the frozen run.** I reviewed all 11 current files, including the concurrently added analyzer, tests, and issue draft. Earlier findings corrected during review are omitted below.

1. **High — Resource failure can escape finalization.** [fault_resources.py:85](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_resources.py:85) stops the sampling thread without checking its final error, while [fault_pager_run.py:328](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_run.py:328) writes completion beforehand. A sampling failure after the last episode check can therefore leave a completed run. The analyzer checks episode `passed` flags but never validates `resources.jsonl` or the final resource receipt. Finalize/check sampling before recording success, and reconcile the raw resource ledger against the existing limits.

2. **Medium — Required allocation breakdown is incomplete.** [fault_pager_run.py:239](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_run.py:239) reports GPU controller tensors but omits the independently retained CPU index—2,877,952 bytes—and page-table/cache metadata. RSS includes these allocations, but cannot supply the separately required accounting. Record both controller copies and metadata residency during the run.

3. **Medium — Resource sampling adds unequal work to timed baselines.** [fault_generation.py:53](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_generation.py:53) invokes `Resources.check()`, which performs and flushes another sample. Consequently, 64 dense output tokens incur 64 synchronous samples, versus 16 rounds for a fully accepting candidate, beyond the prescribed periodic sampling. Separate checking the latched error from taking boundary samples so instrumentation does not introduce this acceptance-dependent timing difference.

4. **High — Page reconciliation accepts execution without page requests.** [fault_pager_analysis.py:60](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_analysis.py:60) checks the declared selection length without reconciling it with demand events. An in-memory probe containing a layer’s 27-page selection and **zero loads/hits** passed `audit_pages`. The audit also receives no policy, so it cannot enforce eager release or LRU/prefetch behavior. Reconcile each layer’s selected pages with its actual demand transactions and validate the declared policy; otherwise incomplete telemetry can appear as byte savings.

5. **Medium — KV audit checks equality rather than the correct boundary.** [fault_pager_analysis.py:95](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_analysis.py:95) accepts any equal target/draft cache lengths. A probe with both lengths zero passed despite requiring length 33. Derive the expected lengths from base, acceptance, fallback consumption, and termination; also derive stop reasons and reject rounds after EOS/cap.

6. **Medium — Matrix validation is not fully anchored to frozen inputs.** [fault_pager_analysis.py:166](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_analysis.py:166) derives expected documents from `reference.json`, rather than requiring the first four diagnostic documents. Target repeats and warmups receive count checks without complete uniqueness/condition checks, and the seeded execution order is not reconciled. Consequently, substituted documents or duplicated warmups can satisfy the inventory checks. Validate the exact frozen document, repetition, warmup, and order identities.

7. **Medium — Generation EOS/accounting branches remain untested in the suite.** [test_fault_generation.py:29](/C:/Users/displ/Documents/dynamic-model-loading/tests/test_fault_generation.py:29) and the new rejection test use out-of-vocabulary EOS `99`. Neither exercises accepted EOS, fallback EOS, or their interaction with remaining allowances. Add deterministic assertions for these cases, four charged proposals, emitted acceptance, and queue reset. The new rejection test addresses the earlier complete absence of rejection coverage.

8. **Medium — Reporting omits specified measurements and the original comparison.** [fault_pager_analysis.py:202](/C:/Users/displ/Documents/dynamic-model-loading/src/dynamic_model_loading/fault_pager_analysis.py:202) reports median episode duration, but not the specified median per-token wall time or demand misses/token. It evaluates ADR 0005’s numerical predicate without reporting the original protocol’s prefetch-versus-LRU physical-improvement decision. These can be computed from retained receipts without changing any frozen criterion.

**Fix 1–3 before inference; resolve 4–8 before accepting the delivery’s conclusions.** The latest focused suite passed **18 tests**. Additional in-memory tiny-model probes matched the dense reference for rejection positions 0–3, fallback EOS, and cap termination. Calibration causality, storage sharing, the 72-episode execution loop, and the AGENTS.md priority change showed no additional concrete defect.

No files were modified and no checkpoint inference was run.