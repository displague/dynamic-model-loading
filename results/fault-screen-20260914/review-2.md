**Approve.** No remaining concrete bugs found in the final fixes. The hook uses `mlp.source`, preserves outputs, and records both layers in the tiny-Qwen test. Prior safeguards and historical 27/32/64 defaults remain intact.

Review only; no edits or inference. The parent’s CPU regression result remains pending.